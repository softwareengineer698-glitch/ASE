rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Any authenticated user can read/write — privacy enforced in app code.
    // The chat screen verifies participantIds before showing messages.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
