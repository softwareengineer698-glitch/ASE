{
  "version": 2,
  "buildCommand": "echo 'Using pre-built web-build directory'",
  "outputDirectory": "web-build",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
